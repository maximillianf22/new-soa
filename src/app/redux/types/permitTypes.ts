
export const permitTypes = {
  // get: '[permits] Get API permits',
  // create: '[permits] Post API new plan',
  // delete: '[permits] Delete API plan',
  // getByAcId: '[permits] Get API permits from account',
  
  loadRedux: '[permits] Load permits into Redux',
  loadReduxFromLogin: '[permits] Load permits from login to reducer',
  addRedux: '[permits] Load new permit to redux',
  deleteRedux: '[permits] Delete permit from redux',
  updateRedux: '[permits] Update permit in redux',

  // update: '[permits] Update plan',
  // selectedPlan: '[permits] Set selected plan',
  // clear: '[permits] Clear data of reducer',
}